defmodule ChatF1 do
  @moduledoc """
  ChatFormula1 — the Elixir/Phoenix GraphQL gateway.

  See `ChatF1.Application` for the supervision tree,
  `ChatF1Web.Schema` for the GraphQL surface,
  and `docs/ARCHITECTURE.md` for the full system design.
  """
end
